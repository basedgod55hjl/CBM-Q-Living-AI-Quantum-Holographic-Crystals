# Nixpacks configuration for Railway deployment
# Crystal Vault SaaS - 7D mH-Q Architecture

[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn backend.app:app --host 0.0.0.0 --port ${PORT:-8000}"

